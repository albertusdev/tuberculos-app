import "package:flutter/material.dart";

class PasienHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Pasien - TuberculosApp")
      ),
    );
  }
}
